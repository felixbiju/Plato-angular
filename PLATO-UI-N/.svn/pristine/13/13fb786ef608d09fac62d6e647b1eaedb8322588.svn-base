import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LiveBuildComponent } from './live-build.component';

describe('LiveBuildComponent', () => {
  let component: LiveBuildComponent;
  let fixture: ComponentFixture<LiveBuildComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LiveBuildComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LiveBuildComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
